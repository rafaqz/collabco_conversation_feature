<?php
/**
 * @file
 * Code for the Collabco Conversation Feature feature.
 */

include_once 'collabco_conversation_feature.features.inc';

function collabco_conversation_feature_views_pre_view($view, $display_id, $args) {
  if ($view->name == 'conversations' && $display_id == 'group_page') {
    collabco_groups_feature_view_content_link($view, 'conversation', 'header');
  }
}

